---
name: clear
description: Reset temporary working context, clean session scratch files, and output zero text. Trigger on /clear or /reset.
---

# Session Clear Workflow (`/clear`, `/reset`)

When `/clear` or `/reset` is triggered:
1. Resets active working memory and temporary scratch files.
2. Output NO text (empty response) so no new text blocks appear in the chat UI.
