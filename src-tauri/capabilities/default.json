{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default local-only desktop permissions for KobeanREST.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "updater:default",
    "log:default",
    "sql:default",
    "stronghold:default"
  ]
}
